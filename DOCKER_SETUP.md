# 🐳 Запуск проекта через Docker

## Быстрый старт

1. **Клонируйте репозиторий:**
```bash
git clone <your-repo-url>
cd coursework-
```

2. **Запустите всё одной командой:**
```bash
docker compose up -d --build
```

3. **Откройте приложение:**
- **Веб-приложение**: http://localhost:3000
- **API**: http://localhost:5000/api
- **PgAdmin**: http://localhost:5050

## 🔑 Данные для входа

### Веб-приложение
- **Администратор**: `admin` / `password`
- **Преподаватель**: `teacher1` / `password`
- **Студент**: `student1` / `password`

### PgAdmin
- **Email**: `admin@example.com`
- **Пароль**: `admin`

### База данных (в PgAdmin)
- **Host**: `db`
- **Port**: `5432`
- **Username**: `postgres`
- **Password**: `123`
- **Database**: `coursework_db`

## 📊 Что включено

- ✅ **Автоматическое создание БД** со всеми таблицами
- ✅ **Тестовые данные**: пользователи, студенты, оценки, прогнозы
- ✅ **Готовые роли**: администратор, преподаватель, студент
- ✅ **Примеры прогнозов** для демонстрации

## 🛠 Управление

### Остановка
```bash
docker compose down
```

### Перезапуск
```bash
docker compose restart
```

### Полная очистка (удалить все данные)
```bash
docker compose down -v --rmi all
```

### Просмотр логов
```bash
docker compose logs -f
```

## 🔧 Полезные команды

### Войти в контейнер БД
```bash
docker compose exec db psql -U postgres -d coursework_db
```

### Войти в контейнер сервера
```bash
docker compose exec server bash
```

### Проверить статус
```bash
docker compose ps
```

## 📝 Примечания

- При первом запуске PostgreSQL автоматически создаст все таблицы и заполнит тестовыми данными
- Все пароли в тестовых данных: `password`
- База данных сохраняется в Docker volume `coursework-_db_data`
- Для продакшена измените пароли в `docker-compose.yml`
