# Веб-приложение для прогнозирования успеваемости студентов

Веб-приложение на React с Node.js и PostgreSQL для прогнозирования успеваемости студентов на основе оценок по экзаменам и результатов зачетов.

## Функциональность

- **Авторизация пользователей** с ролями "студент" и "преподаватель"
- **Управление данными студентов** (группы, ФИО, оценки)
- **Прогнозирование успеваемости** на основе взвешенной формулы: 70% от нормализованной средней оценки по экзаменам и 30% от процента сданных зачетов
- **Разграничение доступа**: преподаватели могут добавлять данные и рассчитывать прогнозы, студенты только просматривают свои данные и прогнозы

## Структура базы данных

### Таблицы:
- `user_roles` - роли пользователей (студент/преподаватель)
- `users` - пользователи системы
- `groups` - группы студентов
- `students` - информация о студентах
- `exams` - экзамены
- `credits` - зачеты
- `exam_grades` - оценки по экзаменам
- `credit_results` - результаты зачетов
- `performance_predictions` - прогнозы успеваемости

### Предметы:
**Экзамены:**
- Программирование
- Схемотехника
- Безопасность операционных систем
- Математическая логика
- Математический анализ

**Зачеты:**
- Философия
- Правоведение
- Основы информационной безопасности
- Теория вероятности
- Дискретная математика

**Группы:**
- АБс-322
- АБс-323
- АБс-324
- АБ-321
- АБ-322

## 🐳 Запуск через Docker (рекомендуется)

### Предварительные требования

- **Docker** версии 20.10 или выше
- **Docker Compose** версии 2.0 или выше

### Быстрый старт

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/Gazolin4ik/coursework-
cd coursework-
```

2. **Запустите всё одной командой:**
```bash
docker compose up -d --build
```

3. **Сгенерируйте тестовые данные (после запуска БД):**
```bash
# Подождите 10-15 секунд пока БД запустится, затем:
docker compose --profile tools run --rm generate-data
```

Или одной командой:
```bash
docker compose up -d db && sleep 15 && docker compose --profile tools run --rm generate-data
```

4. **Откройте приложение:**
- **Веб-приложение**: http://localhost:3000
- **API**: http://localhost:5000/api
- **PgAdmin**: http://localhost:5050

### Данные для входа

**Веб-приложение (после генерации данных):**
- **Администратор**: Username: `admin`, Password: `admin123`
- **Студенты**: Username (например `ivanov_ivan`), Password: `student123`
- **Преподаватели**: Username (например `petrov_petr`), Password: `teacher123`

**PgAdmin:**
- **Email**: `admin@example.com`
- **Пароль**: `admin`

**База данных (в PgAdmin):**
- **Host**: `db`
- **Port**: `5432`
- **Username**: `postgres`
- **Password**: `123`
- **Database**: `coursework_db`

### Управление контейнерами

**Остановка:**
```bash
docker compose down
```

**Перезапуск:**
```bash
docker compose restart
```

**Полная очистка (удалить все данные):**
```bash
docker compose down -v --rmi all
```

**Просмотр логов:**
```bash
docker compose logs -f
```

**Проверить статус:**
```bash
docker compose ps
```

**Войти в контейнер БД:**
```bash
docker compose exec db psql -U postgres -d coursework_db
```

**Войти в контейнер сервера:**
```bash
docker compose exec server sh
```

**Генерация тестовых данных:**
```bash
docker compose --profile tools run --rm generate-data
```

## 💻 Локальный запуск

### Предварительные требования

1. **Node.js** (версия 18 или выше)
2. **PostgreSQL** (версия 15 или выше)
3. **npm** или **yarn**

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/Gazolin4ik/coursework-
cd coursework-
```

### Шаг 2: Установка зависимостей

```bash
# Установка серверных зависимостей
npm install

# Установка клиентских зависимостей
cd client
npm install
cd ..
```

### Шаг 3: Настройка базы данных

1. **Создайте базу данных в PostgreSQL:**
   ```sql
   CREATE DATABASE coursework_db;
   ```

2. **Создайте файл `.env` в корневой директории:**
   ```env
   # База данных
   DB_HOST=localhost
   DB_PORT=5432
   DB_USER=postgres
   DB_PASSWORD=123
   DB_NAME=coursework_db

   # JWT секрет
   JWT_SECRET=your-super-secret-jwt-key

   # Порт сервера
   PORT=5000

   # Режим разработки
   NODE_ENV=development
   ```

3. **Запустите скрипт создания таблиц:**
   ```bash
   npm run setup-db
   ```

   Или вручную выполните SQL-скрипт:
   ```bash
   psql -U postgres -d coursework_db -f database_schema.sql
   ```

4. **Сгенерируйте реалистичные тестовые данные:**
   ```bash
   npm run generate-data
   ```
   
   Это создаст:
   - 50 студентов (по 10 на каждую группу) с регистрацией
   - 10 преподавателей с регистрацией
   - Случайные оценки для студентов
   - Автоматические прогнозы успеваемости
   
   Подробнее см. `GENERATE_DATA_README.md`

### Шаг 4: Запуск приложения

**Вариант 1: Запуск в режиме разработки (в двух терминалах)**

Терминал 1 - сервер:
```bash
npm start
# или с автоперезагрузкой
npm run dev
```

Терминал 2 - клиент:
```bash
cd client
npm start
```

**Вариант 2: Сборка и запуск продакшн версии**

```bash
# Сборка клиента
npm run build

# Запуск сервера
NODE_ENV=production npm start
```

### Доступ к приложению

- **Веб-приложение**: http://localhost:3000
- **API**: http://localhost:5000/api
- **Health check**: http://localhost:5000/api/health

## Структура проекта

```
coursework-/
├── client/                 # React приложение
│   ├── public/
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── pages/         # Страницы приложения
│   │   ├── services/      # API сервисы
│   │   ├── contexts/      # React контексты
│   │   └── App.js
│   ├── Dockerfile
│   ├── nginx.conf
│   └── package.json
├── server/                 # Node.js сервер
│   ├── routes/            # API маршруты
│   ├── middleware/        # Промежуточное ПО
│   ├── utils/             # Утилиты сервера
│   └── server.js
├── docker/                 # Docker скрипты
│   └── server-entrypoint.sh
├── database_schema.sql    # SQL скрипт создания БД
├── init_data.sql          # SQL скрипт начальных данных
├── docker-compose.yml     # Docker Compose конфигурация
├── Dockerfile.server      # Dockerfile для сервера
├── setup_database.js      # Скрипт настройки БД
├── server.js              # Точка входа сервера
├── package.json
└── README.md
```

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/profile` - Получение профиля пользователя
- `PUT /api/auth/profile` - Обновление профиля пользователя

### Студенты
- `GET /api/students` - Получение списка студентов (только преподаватели)
- `GET /api/students/me` - Получение информации о текущем студенте
- `POST /api/students` - Добавление студента (только преподаватели)
- `GET /api/students/:id` - Получение информации о студенте
- `PUT /api/students/:id` - Обновление данных студента (только преподаватели)
- `DELETE /api/students/:id` - Удаление студента (только преподаватели)
- `GET /api/students/groups/list` - Получение списка групп

### Оценки
- `GET /api/grades/student/:studentId` - Получение оценок студента
- `POST /api/grades/exam` - Добавление оценки по экзамену (только преподаватели)
- `PUT /api/grades/exam/:id` - Обновление оценки (только преподаватели)
- `DELETE /api/grades/exam/:id` - Удаление оценки (только преподаватели)
- `POST /api/grades/credit` - Добавление результата зачета (только преподаватели)
- `PUT /api/grades/credit/:id` - Обновление результата зачета (только преподаватели)
- `DELETE /api/grades/credit/:id` - Удаление результата зачета (только преподаватели)
- `GET /api/grades/exams/list` - Получение списка экзаменов
- `GET /api/grades/credits/list` - Получение списка зачетов

### Прогнозы
- `GET /api/predictions` - Получение всех прогнозов (только преподаватели)
- `GET /api/predictions/student/:studentId` - Получение прогноза для студента
- `POST /api/predictions/calculate/:studentId` - Расчет прогноза (только преподаватели)
- `GET /api/predictions/statistics/groups` - Статистика по группам (только преподаватели)
- `GET /api/predictions/history/:studentId` - История прогнозов студента
- `DELETE /api/predictions/:id` - Удаление прогноза (только преподаватели)

### Health Check
- `GET /api/health` - Проверка работоспособности сервера

## Алгоритм расчета прогноза

Прогноз успеваемости рассчитывается по взвешенной формуле: нормализованная средняя оценка по экзаменам (70%) плюс процент сданных зачетов (30%), где оценки экзаменов переводятся из диапазона 2-5 в проценты.

**Формула:**
```
1. Средняя оценка по экзаменам = (сумма всех оценок) / (количество экзаменов)
2. Процент зачетов = (сданные зачеты / всего зачетов) × 100
3. Нормализация экзаменов = ((средняя_оценка - 2) / 3) × 100
4. Общий балл = (нормализованная_оценка × 0.7) + (процент_зачетов × 0.3)
```

## Разработка

### Добавление новых функций

1. Создайте компонент в `client/src/components/`
2. Добавьте маршрут в `client/src/App.js`
3. Создайте API endpoint в `server/routes/`
4. Обновите базу данных при необходимости

### Тестирование

```bash
# Запуск тестов клиента
cd client && npm test

# Запуск тестов сервера
npm test
```

## Лицензия

MIT License

## Поддержка

При возникновении проблем создайте issue в репозитории проекта.

Скрипт `generate_realistic_data.js` создает реалистичные тестовые данные для системы прогнозирования успеваемости студентов.

## Что создается

1. **50 студентов** (по 10 на каждую из 5 групп)
   - Все студенты автоматически регистрируются как пользователи
   - Используются реалистичные русские ФИО
   - Пароль по умолчанию: `student123`

2. **10 преподавателей**
   - Все преподаватели автоматически регистрируются как пользователи
   - Каждому преподавателю прикреплена одна дисциплина (экзамен или зачет)
   - Каждому преподавателю прикреплены все группы
   - Пароль по умолчанию: `teacher123`

3. **Случайные оценки**
   - Оценки по экзаменам (2-5) для 70% студентов
   - Результаты зачетов (сдано/не сдано) для 70% студентов
   - Вероятность сдать зачет: 85%

4. **Прогнозы успеваемости**
   - Автоматически рассчитываются для всех студентов с оценками

## Запуск через Docker (рекомендуется)

**Все работает через Docker, установка Node.js и зависимостей не требуется!**

```bash
# Убедитесь, что БД запущена
docker compose up -d db

# Подождите запуска БД (10-15 секунд)
sleep 15

# Запустите генерацию данных
docker compose --profile tools run --rm generate-data
```

Или используйте удобный скрипт:
```bash
./docker/generate-data.sh
```

**Важно:** Все операции выполняются через Docker, никаких дополнительных установок не требуется!

Подробнее см. `DOCKER_USAGE.md`