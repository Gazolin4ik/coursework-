# Использование через Docker

Все операции выполняются через Docker, установка зависимостей не требуется!

## Быстрый старт

### 1. Запуск всего приложения

```bash
docker compose up -d --build
```

Это запустит:
- Базу данных PostgreSQL
- Сервер Node.js
- Клиент React
- PgAdmin (опционально)

### 2. Генерация тестовых данных

После запуска приложения, сгенерируйте данные:

```bash
docker compose --profile tools run --rm generate-data
```

Или одной командой:

```bash
docker compose up -d db && sleep 10 && docker compose --profile tools run --rm generate-data
```

## Полная последовательность

```bash
# 1. Клонирование репозитория (если нужно)
git clone <repository-url>
cd coursework-

# 2. Запуск всех сервисов
docker compose up -d --build

# 3. Ожидание запуска БД (10-15 секунд)
sleep 15

# 4. Генерация тестовых данных
docker compose --profile tools run --rm generate-data

# 5. Проверка работы
# Откройте браузер: http://localhost:3000
```

## Управление контейнерами

### Остановка всех сервисов
```bash
docker compose down
```

### Остановка с удалением данных
```bash
docker compose down -v
```

### Просмотр логов
```bash
# Все сервисы
docker compose logs -f

# Конкретный сервис
docker compose logs -f server
docker compose logs -f db
```

### Перезапуск сервиса
```bash
docker compose restart server
```

## Генерация данных

### Первая генерация
```bash
docker compose --profile tools run --rm generate-data
```

### Повторная генерация (старые данные удалятся)
```bash
docker compose --profile tools run --rm generate-data
```

### Генерация данных при первом запуске (автоматически)

Можно добавить в `docker/server-entrypoint.sh` автоматическую генерацию, но это не рекомендуется, так как данные будут генерироваться каждый раз при перезапуске.

## Доступ к приложению

После запуска:

- **Веб-приложение**: http://localhost:3000
- **API сервер**: http://localhost:5000
- **PgAdmin**: http://localhost:5050

## Данные для входа

После генерации данных:

**Администратор:**
- Username: `admin`
- Password: `admin123`

**Студенты:**
- Username: `ivanov_ivan` (пример, зависит от сгенерированного ФИО)
- Password: `student123`

**Преподаватели:**
- Username: `petrov_petr` (пример)
- Password: `teacher123`

## Устранение проблем

### Ошибка: "Cannot find module"
**Решение:** Пересоберите образы:
```bash
docker compose build --no-cache
```

### Ошибка: "Connection refused" при генерации данных
**Решение:** Убедитесь, что БД запущена:
```bash
docker compose up -d db
sleep 10
docker compose --profile tools run --rm generate-data
```

### Ошибка: "database does not exist"
**Решение:** БД создается автоматически, но структура таблиц создается при первом запуске. Если нужно создать вручную:
```bash
docker compose exec server node setup_database.js
```

### Очистка и пересоздание всего
```bash
# Остановить и удалить все
docker compose down -v

# Пересоздать
docker compose up -d --build

# Подождать запуска БД
sleep 15

# Сгенерировать данные
docker compose --profile tools run --rm generate-data
```

## Полезные команды

### Вход в контейнер БД
```bash
docker compose exec db psql -U postgres -d coursework_db
```

### Вход в контейнер сервера
```bash
docker compose exec server sh
```

### Просмотр статуса
```bash
docker compose ps
```

### Использование ресурсов
```bash
docker stats
```

## Для других устройств

Просто скопируйте проект и выполните:

```bash
# Убедитесь, что Docker и Docker Compose установлены
docker --version
docker compose version

# Запустите всё одной командой
docker compose up -d --build

# Подождите и сгенерируйте данные
sleep 15
docker compose --profile tools run --rm generate-data
```

**Никаких дополнительных установок не требуется!** 🎉

