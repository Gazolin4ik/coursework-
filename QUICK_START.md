# Быстрый старт через Docker

## 🚀 Запуск за 3 команды

```bash
# 1. Запустите всё приложение
docker compose up -d --build

# 2. Подождите 15 секунд (пока БД запустится)
sleep 15

# 3. Сгенерируйте тестовые данные
docker compose --profile tools run --rm generate-data
```

Готово! Откройте http://localhost:3000

## 📋 Что создается

- **1 администратор**
  - Username: `admin`, Пароль: `admin123`
- **50 студентов** (по 10 на каждую группу)
  - Пароль: `student123`
- **10 преподавателей**
  - Пароль: `teacher123`
- Случайные оценки и прогнозы

## 🔑 Вход в систему

После генерации данных используйте:
- **Администратор**: username `admin`, пароль `admin123`
- **Студенты**: любой username (например `ivanov_ivan`), пароль `student123`
- **Преподаватели**: любой username (например `petrov_petr`), пароль `teacher123`

## 🛠 Управление

```bash
# Остановить всё
docker compose down

# Просмотр логов
docker compose logs -f

# Пересоздать данные
docker compose --profile tools run --rm generate-data
```

## ⚠️ Важно

**Никаких установок не требуется!** Только Docker и Docker Compose.

Подробная документация: `DOCKER_USAGE.md`

